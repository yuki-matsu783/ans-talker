---
title: worklog 【実装】【テスト】受講者ソースの受け渡し push1
type: log
description: issue #6 のフェーズ3の2周目（実装・テスト）の記録。
tags: [worklog, answer-talker, 実装, テスト]
keywords: [submission, 縮退, degraded, 単体テスト, 17パターン, 進捗表, 2周目]
---

# worklog: 【実装】【テスト】受講者ソースの受け渡し（push1）

対象: issue #6 のフェーズ3の**2周目**（2026-08-21）。
全体作業計画: `plans/bubbly-exploring-biscuit.md`
個別作業計画: `plans/【実装】【テスト】受講者ソースの受け渡し.md`
設計（前提）: `reports/20260821_bubbly-exploring-biscuit_受講者ソースの受け渡しの設計結果.md`
push回数: 9

## 試したこと

- flow-id 3-10 まで終えて `【設計】` を閉じ、**フェーズ3をもう1周する**形で
  `【実装】【テスト】` の計画を作った。
- **進捗表の扱いに専用の仕組みが無かった。** `update-handoff-progress.sh` は
  `mark-done` / `mark-skip` / `add-round` / `set-header` の4つで、「フェーズを丸ごともう1周する」
  操作は持っていない。次のように分けて対処した。
  - ループ範囲（3-3〜3-4 / 3-6〜3-9）は **`add-round`** で2周目へ進めた（記号が `[]` へ戻る）。
  - 単発ステップ（3-1・3-2・3-5・3-10）は **`sed` で手作業**で `[]` へ戻した。
  - ヘッダの `- 現在のループ:` は `set-header --loop` で現状を書いた。
- 併記（`【実装】` と `【テスト】` を1ファイル）にした。設計で決着済みのため、**実装とテストの
  間に人間の合意を挟む必要が無い**と判断した。ただし17パターン検証は工数が大きいため、
  途中で分割してよいと計画へ明記した。

## うまくいったこと

- **設計結果から「実装で落としやすい点」をチェックリストとして転記できた。** 2回のレビューで
  出た指摘（段2の対象パスをdiffから作らない・`tmpdir` を返す・手順7へ引数を渡す等）は、
  どれも**実装時に落とすと無言で壊れる**ものなので、計画の側にチェックボックスとして持たせた。
- **検証節を「動くか・決まっているか」を検出する形にできた。** `【設計】` のレビューで
  「一般的な語を数えるだけの検証コマンドは常に成功する」と指摘されたため、今回は
  (1) `NG=0` の判定、(2) 新規テストの `passed` が二桁以上、(3) `bash -n`、
  (4) **3段すべてを人工的に落として実際に走らせる**、(5) 縮退時のキーの確認、で構成した。

## ダメだったこと・気づき

- **`mark-done 2-6` / `mark-done 3-6` の罠を1回踏んだ**（記録は `【設計】` のworklog）。
  作業を終えた直後に記号を進めたくなるが、ループ範囲は**1周が完全に完了して初めて `[x]`**。
- **進捗表が「フェーズを複数回まわる」ことを表現できない。** 今回のように
  `【設計】` → `【実装】【テスト】` と個別計画を分けると、3-1〜3-10 を2回通ることになるが、
  表は1セットしか持たない。`add-round` はループ範囲にしか効かず、単発ステップは手で戻すしかない。
  **フェーズ4のAIアセット反映の候補**（`update-handoff-progress.sh` に「フェーズの周回」を
  足すか、`HANDOFF.md` の書き方で吸収するか）。

## 次にやること

- flow-id 3-2: commit・リモートへ反映してレビュー依頼。
- flow-id 3-3〜3-4: 実装計画のレビュー（**敵対的レビューはフェーズ3で残り1回**）。
- 合意後、flow-id 3-6 で実装・テストを実施する。
