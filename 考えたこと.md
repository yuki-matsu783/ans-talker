# AIエージェント開発フロー 設計思想

AIアセット（スキル・ルール・スクリプト）を整備するにあたって考えたことと、その理由をまとめる。

---

## 1. 階層構造はまだ早い

**目指す状態**: git cloneしてきたプロジェクトをそのままClaude Codeに渡せば、すぐ使えること。

当初は、ユーザーホームの `~/.claude` に共通的なスキル、プロジェクトルートの `.claude` にプロジェクト固有のスキルを置く階層構造を検討した。しかし次の理由で見送り、**すべてプロジェクト内に含める**構成にした。

- 利用開始までの環境構築手順が増える
- ユーザーホームの書き換えを嫌う人がいる
- CI/CDで使いづらい

**保留事項**: 一方で、パートナーさんとか納品対象とかにするってなるとまた違う問題が出てくるので、プロジェクトに適用ってなったら改めて考えたい。(これを組み込んだClaude Codeをビルドして渡すとか？)

---

## 2. 依存するものは最低限にする

MCPを使って進捗管理を外部ツールに任せることもできるが、その分の環境構築手順がかかり、適用できないプロジェクトも出てくるため避けた。

**情報管理をすべてgitに一本化**し、gitとVCS（GitHub/GitLab）さえあればフローが成立するようにしている。極端な話、ローカルにGitLabのDockerコンテナを立てれば、完全に独立した環境でも動作させられる（重いが）。

---

## 3. 普段やってるワークフローに載せる

新しい作法を覚えてもらうのではなく、既にやっている開発フローにそのまま乗せる。

```
issueを作る（例: XXXAPIを開発する）
  ↓
コミュニケーションツールで開発を依頼(mattermostやzoom会話など)
  ↓
マージリクエスト作成 & featureブランチ作成
  ↓
開発途中の断面を任意のタイミングでpushし、レビュー依頼
  ↓
レビューをコメントし、コミュニケーションツールで連絡
  ↓
レビュー内容を確認 → 修正 → push → レビュー依頼（指摘がなくなるまで繰り返す）
  ↓
最終的な内容を確認してdefaultにマージ
```

このうち、**コミュニケーションツールの箇所がAIへのプロンプトに置き換わった**というのが基本イメージ。issue・MR・レビューコメントといった既存の道具立ては何も変わらない。

---

## 4. フロー情報と確定情報をどう管理するか

計画や作業ログといった「フロー情報」は、作業中には必要だが、最終的にgitには残したくない。迷ったが、次の形に落ち着いた。

- **featureブランチには載せる**（セッションをまたいだ引き継ぎに必要なため）
- **マージ前に削除してお片付けする**
- マージは **squash merge** で行う。途中断面はdefaultブランチの履歴に入らないため、git cloneが必要以上に重くなることはない

フロー情報のうち後に残したいものは、削除する前にそれぞれ下記に反映するというルールにする。

| 残したいもの | 置き場所 |
|---|---|
| 仕様として確定した内容 | 設計ドキュメント |
| 判断の経緯 | DDR（Design Decision Record） |
| 汎用的な知見 | スキル |

---

## 5. スキルの評価方法

改訂されたスキルが本当に価値のあるものなのか、評価する手立てが無いのが課題だった。案として、**AIアセットだけが異なる2つのブランチでA/B比較する**方法を考えた。

例えば、developブランチで育てたスキルをmainにマージしてよいか迷ったとき：

1. まず、**AIアセット以外**をmainにマージする
2. AI評価用のissueを作成する（例: `新たにYYYをするAPIを作成する`）
3. そのissueをもとに、mainから `feature-ai-old`、developから `feature-ai-new` をそれぞれ作成し、AIに開発してもらう
4. レビュー等のプロセスを経て、**new側の方が価値のあるものができていれば**、AIアセットもmainにマージする

さらに、この流れ自体を実行する評価用スキルも作る。メインエージェントがサブエージェントAにoldアセットでの開発を、サブエージェントBにnewアセットでの開発をそれぞれ依頼し、できたものを比較する形になる。
