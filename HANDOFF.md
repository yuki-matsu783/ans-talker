---
title: HANDOFF
type: handoff
description: セッション間・作業者間の引継ぎメモ（現在地・次回やること等）
tags: [handoff, workflow]
keywords: [フロー進捗, worklog, 引き継ぎ, plan, レビュー]
---

# HANDOFF

<!--
AI⇔AI/AI⇔人間の状況引継ぎメモ。常に「このブランチの現状」を表現する
-->

## フロー進捗状況

- issue: #1
- ブランチ: `feature-1-add-answer-talker-review-skill`
- PR: #2（https://github.com/yuki-matsu783/ans-talker/pull/2 ）
- push回数: 7
- 現在のループ: 3-6〜3-9 の1周目（進行中）
- 追従監視: なし（ローカル。各pushとflow-id 5-2で手動確認する）

| 進捗 | flow-id | ステップ | 担当 |
|----|---|---|---|
| [x] | 1-1 | issueを起票する | 人間 |
| [x] | 1-2 | issueの内容を取得する | サブコマンド |
| [x] | 1-3 | featureブランチとDraft MRを作成する | サブコマンド |
| [x] | 1-4 | Planモードで全体作業計画を作成する | エージェント |
| [x] | 1-5 | 全体作業計画に合意する | 人間 |
| [x] | 1-6 | 全体作業計画をもとにHANDOFF.mdを更新する | エージェント |
| [x] | 2-1 | 個別調査計画を作成する（worklogも作成） | エージェント |
| [x] | 2-2 | commit・pushしてレビュー依頼を行う | エージェント |
| [x] | 2-3 | 調査計画をレビュー・コメントする | 人間 |
| [x] | 2-4 | レビュー内容を取得し調査計画を修正する | サブコマンド |
| [x] | 2-5 | 調査計画をもとにMR descriptionを更新する | サブコマンド |
| [x] | 2-6 | 調査を実施しreports/へ記録する | エージェント |
| [x] | 2-7 | commit・pushしてレビュー依頼を行う | エージェント |
| [x] | 2-8 | 調査結果をレビュー・コメントする | 人間 |
| [x] | 2-9 | レビュー内容を取得し調査結果を修正する | サブコマンド |
| [x] | 2-10 | 調査結果をもとにMR descriptionを更新する | サブコマンド |
| [x] | 3-1 | 個別作業計画を作成する | エージェント |
| [x] | 3-2 | commit・pushしてレビュー依頼を行う | エージェント |
| [x] | 3-3 | 作業計画をレビュー・コメントする | 人間 |
| [x] | 3-4 | レビュー内容を取得し作業計画を修正する | サブコマンド |
| [x] | 3-5 | 作業計画をもとにMR descriptionを更新する | サブコマンド |
| [] | 3-6 | 作業を進めreports/へ結果を記録する | エージェント |
| [] | 3-7 | commit・pushしてレビュー依頼を行う | エージェント |
| [] | 3-8 | 実装をレビュー・コメントする | 人間 |
| [] | 3-9 | レビュー内容を取得し実装・ドキュメントを修正する | サブコマンド |
| [] | 3-10 | 作業内容をもとにMR descriptionを更新する | サブコマンド |
| [] | 4-1 | 個別反映計画を作成する（反映対象の洗い出し） | エージェント |
| [] | 4-2 | commit・pushしてレビュー依頼を行う | エージェント |
| [] | 4-3 | 反映計画をレビュー・コメントする | 人間 |
| [] | 4-4 | レビュー内容を取得し反映計画を修正する | サブコマンド |
| [] | 4-5 | 反映計画をもとにMR descriptionを更新する | サブコマンド |
| [] | 4-6 | 設計反映・AIアセット反映を実施する | エージェント |
| [] | 4-7 | commit・pushしてレビュー依頼を行う | エージェント |
| [] | 4-8 | 反映内容をレビュー・コメントする | 人間 |
| [] | 4-9 | レビュー内容を取得し設計・AIアセットを修正する | サブコマンド |
| [] | 4-10 | 反映内容をもとにMR descriptionを更新する | サブコマンド |
| [] | 5-1 | plans/worklog/reportsを片付けHANDOFF.mdをリセットする | エージェント |
| [] | 5-2 | defaultブランチとのコンフリクトを検知・解消する | エージェント |
| [] | 5-3 | 関連issueへマージ前通知を行う | エージェント |
| [] | 5-4 | commit・pushしてDraftを解除する | エージェント |
| [] | 5-5 | マージする（squash merge） | 人間 |

## やったこと

issue #1（演習MRを正解ソースと照合してレビューする answer-talker スキルを追加する）に着手した。

- `/issue-mr-flow start 1` を実行し、issue本文を取得した。標準4見出し（目的・現状・期待する動作・
  受け入れ条件）はすべて揃っていることを `test_issue_sections` で確認済み。
- 既存の `feature-1-*` ブランチはローカル・リモートともに無かったため、ベースブランチ `main` から
  `feature-1-add-answer-talker-review-skill` を新規作成し、Draft PR #2 を作成した
  （ベースブランチとDraft PR作成はユーザー承認済み）。
- 全体作業計画 `plans/bubbly-exploring-biscuit.md` をPlanモードで作成し、ユーザーの承認を得た。
  - **issueの粒度判定**: 分割は提案しない（受け入れ条件6項目は同型の成果物の並列列挙ではなく、
    単独マージすると壊れる横断的変更のため）。
  - フェーズ2〈調査〉は実施する。フェーズ4〈反映〉は flow-id 4-1 で反映対象を洗い出す。
- flow-id 2-1: 個別調査計画 `plans/【調査】answer-talkerスキルの設計調査.md` を
  **planツールを使わず**Write/Editで作成した（6論点。全体作業計画の5論点に、実例確認の題材を
  どう用意するかを論点6として追加した）。あわせて
  `worklog/20260820_bubbly-exploring-biscuit_【調査】answer-talkerスキルの設計調査_push1.md` を作成した。
- flow-id 2-3〜2-4: 「レビューOK」を受け、`get_mr_unresolved_comments 2 true` で全件取得して
  未解決スレッドが0件であることを確認した。レビュー依頼本文で問いかけた論点6の扱いが承認された形に
  なるため、その判断をPRへコメントとして記録した（`#issuecomment-5355515953`）。
- flow-id 2-5: MR descriptionを全体作業計画・個別調査計画の要約へ更新した。
- flow-id 2-6: 調査を実施し、結果を
  `reports/20260820_bubbly-exploring-biscuit_answer-talker設計調査.md`（正文）と同名の `.html` へ
  記録した。要点は次の3つ。
  - `adversarial-review` との構造的な違いは「自分のブランチのMR」対「引数で受けた他人のMR」の
    1点に集約でき、diff取得関数が無いこと・上限機構が合わないこと・観点表を使わないことが
    すべてここから導ける。**実施回数の上限機構は課さない**（DDR候補）。
  - diff取得は `gh api repos/{owner}/{repo}/pulls/<n>/files` を採る（既存の投稿処理と情報源が
    一致し、`status` が取れるため）。PR #2 に対して実機確認済み。
  - 正解ソースは**ローカルパスならcloneしない**（`--depth 1` が局所cloneで無視されることと、
    素のディレクトリはcloneできないことを実機で確認）。URLのみ shallow clone し `trap` で後始末する。
- flow-id 2-8〜2-9（レビュー指摘①）: 「GitLabのdockerコンテナを立てているので検証できるはず」との
  指摘を受け、**GitLab経路を実機検証した**（GitLab CE 18.5.4-ce.0 @ `localhost:8929` ／
  `glab` 1.114.0 ／ 既存の検証用MR `root/issue45-verify!3`）。判断はPRへ記録した
  （`#issuecomment-5355761434`）。
  - `/diffs`（新API）を採る。`/changes` は切り捨てがMR全体単位（`overflow`）でページングも無いため。
  - **共通形への正規化が両プロバイダで成立することを実データで確認**（キー集合が一致）。
  - プロバイダ間で埋める差は2つだけ: `status` の語彙変換と、**GitLabが返さない
    `additions`/`deletions`**（diff本文の行頭を数えて算出。実データで検算済み）。
  - **未決定事項#1（GitLab対応の扱い）は解消**し、GitLabも対象に含めて実装する方針とした。
- flow-id 2-8〜2-9（レビュー指摘②）: MRの未解決スレッド1件
  （`#discussion_r3821504030`）で「演習用プロジェクトを1つ作って実投稿まで確認する」との指摘を受け、
  論点6の結論を差し替えた。返信済み（`#discussion_r3821524036`）。
  - 実例確認は**ローカルGitLabに演習用プロジェクトを1つ作り、実投稿まで行う**。当初の
    「投稿直前で止める」提案は取り下げた。
  - これにより、論点2で未確認だった**GitLab側の投稿処理**（`gitlab_add_mr_inline_comments`,
    `Gitlab.sh:360`）も同時に確認できる。論点2で実機確認できたのは差分の取得までだった。
  - 題材はこのリポジトリに置かない（plugin配布物にサンプルアプリを混ぜない）。
  - **未決定事項#6は解消**。人間の判断待ちの項目は無くなった。
- flow-id 2-6〜2-9 完了: 「レビューOK」を受けて `comments all` でスレッドを再取得し、
  **未解決0件・解決済み2件**を確認したうえでループを完了にした。
- flow-id 2-10: MR descriptionを調査結果込みの内容へ更新した。
- flow-id 3-1: 個別作業計画 `plans/【設計】answer-talkerスキルの構成とインターフェース.md` を作成した
  （`【実装】【テスト】` とは分ける。インターフェースが決まらないと6ファイルに波及するため、
  実装前に合意を取る）。決めることを **D1〜D7** として洗い出し、各項目を
  「決める内容」「判断軸」の2点構成にした。調査の未決定事項4件はD2（2件）・D3（1件）・D5（1件）へ
  割り付け済み。
- flow-id 3-3〜3-4: レビューで2点の判断を受け、設計計画へ反映した
  （記録: `#issuecomment-5356032379`）。
  - **D5: ネタバレ検査は誤検知の側へ倒す**（正当な指摘を落としてでも転記の見逃しを避ける）。
    「前提（合意済みの制約）」節を新設し、判断軸を確定させた。あわせて、この向きの失敗の仕方
    （指摘が落ちすぎる）に備え、落とした件数と反応した語の可視化を設計項目へ追加した。
  - **D7: 検証環境はいろいろなパターンを試せる構成にする**。5軸15パターンの表へ全面的に
    書き換えた（受講者の実装／正解ソースの形態／diffの内容／ネタバレ検査／投稿）。
  - 2つの指摘は噛み合っていた。誤検知に倒す以上、**一般語のみの正当な finding が落とされない**
    ことを確かめる経路が要るため、D7にパターン⑬として追加した。
  - 判断軸に「**指摘しないことを確かめるパターンが含まれているか**」を追加した（②別解・⑤ほぼ
    正解・⑬一般語のみ・⑮0件）。指摘が出ることだけを確かめる検証は、過剰に指摘するスキルを
    合格させてしまうため。
  - 調査で未確認だった**リネーム時の挙動**を、パターン⑩として検証環境に組み込んだ。
- flow-id 3-5: MR descriptionを設計計画込みの内容へ更新した。
- flow-id 3-6: D1〜D7の設計を実施し、結果を
  `reports/20260820_bubbly-exploring-biscuit_answer-talker設計.md`（正文）と同名の `.html` へ記録した。
  - **調査結果に誤りがあったので訂正した。** 「`add_mr_inline_comments` はMR番号を引数で取るので
    他人のMRにもそのまま使える」は誤り。`gh`/`glab` は**対象リポジトリをcwdのgitリモートから
    解決する**ため、MR番号だけでは対象が決まらない（実機で確認）。
  - 解決策として **`GH_REPO` / `GITLAB_REPO` の環境変数で解決先を上書きできる**ことを実機で確認した。
    **既存の `Provider.sh`/`Github.sh`/`Gitlab.sh` を1行も変えずに済む**ため、引数追加案
    （`adversarial-review` の呼び出しまで波及）より優れる。`glab api` に `-R` が無いことも確認済み。
  - 追加する関数は `use_target_repo` と **`get_mr_changed_files`**（`get_mr_diff` にしない。既存の
    `get_mr_diff_url` と似た名前で中身が違う3兄弟になるため）。返却はstdout、サイズ対策は
    「呼び出し側が必ずファイルへリダイレクトする」規約で担保する。
  - **調査時点の未決定事項4件はすべて決定**。新たに3件（`use_target_repo` の置き場所／除外語の
    初期内容／検証セットアップを `.claude/` に残すか）が生じた。

## 次にやること

- flow-id 3-8: 設計結果のレビュー待ち（人間）。とくに次の2点を見てほしい。
  - 対象リポジトリの切り替えを**環境変数で行う**方針（既存の共通コードに触らない代わりに、
    プロセスの状態に依存する）
  - D5で**識別子を分割しない**と決めたこと（誤検知に倒す方針の中で、ここだけ厳しくしない判断）
- 合意後、`plans/【実装】【テスト】〜.md` を作成してフェーズ3の2セット目に入る。
  **検証手順には、ローカルGitLabの演習用プロジェクト構築（初期状態・受講者MR・正解ソース）と
  実投稿までの確認を含める（15パターン）。**

## 判断を迷った内容

- `HANDOFF.md` が、テンプレート取り込み元リポジトリ（MR-driven-workflow）のissue #117 の内容を
  保持したままだった（flow-id 5-1 のリセットを経ずに取り込まれたため）。本ブランチの現在地とは
  無関係な内容であり、「常にこのブランチの現状を表現する」という原則に従って**本issueの内容へ
  全面的に置き換えた**。取り込み元の記録は当該リポジトリ側に残っているため、失われる情報はない。

## 未解決の内容

**調査時点の未決定事項6件はすべて決着した**（#1・#6はレビューで、#2〜#5は flow-id 3-6 の設計で）。

設計で**新たに生じた未決定事項3件**（設計結果mdの該当節）。

- **N1**: `use_target_repo` を `Provider.sh` に置くか、answer-talker専用スクリプトに置くか。
  現状 answer-talker しか使わないため、`Provider.sh` 前提で実装し、据わりが悪ければ再検討する。
- **N2**: 除外語リストの初期内容。実装時に検証パターン⑬で調整する（最初から網羅しようとしない）。
- **N3**: 検証用セットアップスクリプトを `.claude/` に残す必要があるか。flow-id 4-1 で判断する。

実機で確認できていない事項も残る。GitHubの3000ファイル上限・`patch` 省略、GitLabの
`too_large`/`collapsed` が立つ規模。いずれも実装時に「検知して報告する」形で扱い、無言で
切り捨てない。**リネーム時の挙動は検証パターン⑩で潰す。**

## 守るべき条件・触ってはいけない範囲

- 追加する成果物は `.claude/` 配下（plugin配布単位）に収める。`.claude/` の外にスクリプト・
  設計書を置かない（`.claude/rules/directory-structure.md`「配置の指針」）。
- **正解のコード片をレビューコメントへ転記しない**という制約は本issueの中核要件であり、
  サブエージェント側の規約と投稿直前の機械的検査の**二重**で担保する（片方だけにしない）。
- コミットはすべて `commit` スキル経由（`git commit` の直接実行はPreToolUse hookでブロックされる）。
- マージ（flow-id 5-5）はユーザーの明示指示があるまで行わない。
